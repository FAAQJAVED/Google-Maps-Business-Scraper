# Google Maps Business Scraper — Development / CI Dependencies
# ─────────────────────────────────────────────────────────────────
# Exact pins for reproducible CI runs and local development.
# Install: pip install -r requirements-dev.txt
#
# Regenerate pins after upgrading:
#   pip install pip-tools
#   pip-compile requirements.txt --output-file requirements-dev.txt

-r requirements.txt

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0
