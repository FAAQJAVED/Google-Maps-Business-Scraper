# Google Maps Business Scraper — Runtime Dependencies
# ─────────────────────────────────────────────────────────────────
# Install:          pip install -r requirements.txt
# Install browser:  playwright install chromium
#
# Python 3.10 or newer required.

playwright>=1.44.0,<2.0
requests>=2.31.0,<3.0
PyYAML>=6.0.1,<7.0
urllib3>=2.0.0,<3.0

# Excel output (openpyxl is included in the standard install command;
# listed here so pip install -r requirements.txt covers all features)
openpyxl>=3.1.0